public interface State {
    /**
     *
     * @param manager  is used for setting the next state
     */
    void exercise(Manager manager);
    /**
     *
     * @param manager  is used for setting the next state
     */
    void hardWork(Manager manager);
    /**
     *
     * @param manager  is used for setting the next state
     */
    void gtx1080(Manager manager);
    /**
     *
     * @param manager  is used for setting the next state
     */
    void cheating(Manager manager);
    /**
     *
     * @param manager  is used for setting the next state
     */
    void outtilllate(Manager manager);
    /**
     *
     * @param manager  is used for setting the next state
     */
    void sleep(Manager manager);
    /**
     *
     * @param manager  is used for setting the next state
     */
    void coffeeandwork(Manager manager);
}
